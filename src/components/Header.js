import React from 'react'

const Header = () => {
  return (
    <nav>
        <div class="nav-wrapper teal">
            <a href="#" class="brand-logo">React Calculator | FreeCodeCamp Project</a>
        </div>
    </nav>
  )
}

export default Header
